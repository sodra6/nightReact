import React from "react";
import Bts from "./bts.json";

//서버환경에서 DB를 노드서버와 연결하듯 데이터서버와 연결해야 하는데, 서버를 따로 구성해서 실행해야함
//가상데이터 서버를 만들어서 코드만 구현
//json-server : 가상데이터를 만들어주는 라이브러리
//1. yarn : yarn global add json-server
//2. npm : npm install -g json-server

//가상데이터로 만들고싶은 json파일을 루터폴더에 저장(src 폴더 밖에 저장)

function App(props) {
  return <div></div>;
}

export default App;
